(function() {
	/*
	 * 定義一個模塊
	 * 
	 * fn module
	 * 參數-1：應用名稱
	 * 參數-2：可能依賴的其它庫（多個）
	 */
	var app = angular.module('bookcase', ['calendar']);
	
	// app.controller('BookListCtrl', function() {
	// 	this.books = books;
	// });

})();